# Servidor
server.port=8080
server.servlet.context-path=/api

# Banco de Dados (H2 para desenvolvimento)
spring.datasource.url=jdbc:h2:mem:estacionamentodb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Logging
logging.level.com.estacionamento_api.estacionamento=DEBUG

# JWT
# Em produção, isso deve vir de uma variável de ambiente, nunca hardcoded
# no arquivo de propriedades versionado no git.
jwt.secret=chave-secreta-de-desenvolvimento-troque-isso-em-producao-1234567890abcdef
jwt.expiracao-ms=3600000